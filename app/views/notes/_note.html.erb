
    <div class="tan" id="<%= note.persisted? ? dom_id(note) : "note-new" %>">
      <ul id="list">
        <li><b><%= note.text %></b></li>
        <li><span class="asked_by"><%= "Submitted by #{note.user.name} #{note.created_at.strftime("on %m/%d/%Y at %I:%M%p")}" %> </span></li>
      </ul>
    </div>
