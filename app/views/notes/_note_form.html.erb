<!--
1) need to add the "f." back to each field and button
-->
  <!--<# if logged_in? %>
    <#= form_for([@problem, @problem.notes.build]) do |f| %>-->
      <%= label :text, :placeholder => "add your answer here" %>
      <%= text_area :text, :placeholder => "add your answer here" %>
      <%= button_tag 'Answer' %>
    <!--<# end %>

  <# else %>
    <#= link_to 'Login to answer this question', new_login_path %>
  <# end %>-->